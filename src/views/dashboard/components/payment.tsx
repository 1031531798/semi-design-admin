import {Card} from "@douyinfe/semi-ui";
import EChart from "../../../components/chart/echart";
import {EChartsOption} from "echarts";

const Payment = () => {

    return (
        <Card title='Payment' className={'w-full flex flex-col flex-grow mt-5'} bordered={false} headerLine={false} bodyStyle={{
            width: '100%',
            height: '100%'
        }}>
        </Card>
    )
}
export default Payment
